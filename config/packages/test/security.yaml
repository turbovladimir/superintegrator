security:
    providers:
        in_memory: { memory: null }
        users_in_memory: { memory: null }
        users:
            entity:
                class: App\Entity\UserRole
                property: name
    encoders:
        App\Entity\UserRole:
            algorithm: auto

    firewalls:
        main:
            pattern: '^/'
            security: false
